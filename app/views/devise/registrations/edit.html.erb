<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-4 col-sm-offset-4">
      <div class="form-devise">
        <h2>Edita tu información</h2>
        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
          <%= f.error_notification %>
          <div class="form-inputs">
            <%= f.input :email, required: true, autofocus: true %>
            <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
              <p>Esperando confirmación de : <%= resource.unconfirmed_email %></p>
              <% end %>
            <%= f.input :password,
                        hint: "Ignora este campo si no quieres cambiar tu contraseña",
                        required: false,
                        input_html: { autocomplete: "new-password" } %>
            <%= f.input :password_confirmation,
                        required: false,
                        input_html: { autocomplete: "new-password" } %>
            <%= f.input :current_password,
                        hint: "Necesitamos tu contraseña actual para efectuar cambios",
                        required: true,
                        input_html: { autocomplete: "current-password" } %>
          </div>
          <div class="form-actions">
            <%= f.button :submit, "Actualizar", class: 'btn-submit' %>
          </div>
        <% end %>

        <h5>Eliminar cuenta</h5>

        <p>No quisiéramos que te vayas, ¿Quieres eliminar tu cuenta? - <%= link_to "Cancelar cuenta", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

        <%= link_to "Volver", :back %>
        </div>
     </div>
     <br>


    </div>
  </div>
</div>
